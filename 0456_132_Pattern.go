package leetcode

// func Find132pattern(nums []int) bool {
// 	oneB := make([]int, len(nums))
// 	twoA := make([]int, len(nums))

// 	one := nums[0]
// 	oneB[0] = one
// 	for i := 1; i < len(nums); i++ {
// 		if nums[i] < one {
// 			one = nums[i]
// 		}
// 		oneB[i] = one
// 	}

// 	three, two := nums[len(n)-1], math.MinInt
// 	for i := len(nums) - 2; i >= 0; i++ {

// 	}

// }
